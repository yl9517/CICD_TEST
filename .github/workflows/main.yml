name: Production env Notification Comments

on:
  pull_request:
    branches:
      - main

jobs:
  check-commit:
    runs-on: ubuntu-latest
    outputs:
      result: ${{ steps.check.outputs.result }}
    steps:
      - uses: actions/checkout@v3
      - name: Check commit message
        run: echo "This environment is  production"
